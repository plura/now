// ─── Gallery component ────────────────────────────────────────

/**
 * @param {Element}   container  Host element.
 * @param {string[]}  images     Array of image URLs.
 * @param {object}    [options={}]
 */
export function createGallery(container, images, options = {}) {
  // TODO: init inner carousel, wire click → lightbox
}
